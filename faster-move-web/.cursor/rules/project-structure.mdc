---
description: 
alwaysApply: true
---

# 项目结构指南

## 工作区结构（前后端区分）

当前工作区 **FASTMOVE** 为多根工作区，包含：

| 项目 | 类型 | 说明 |
|------|------|------|
| **faster-move-web** | 前端 | Vue + Vite + Electron，路径 `f:\waimaiguang\faster-move-web` |
| **Alien** | 后端 | C# 后端项目，与前端平级 |

协作时需根据用户提问或当前打开的文件判断针对的是前端还是后端，避免混淆。

## 主要目录结构

- `src/`: 源代码主目录
  - `api/`: API 接口定义和请求
  - `assets/`: 静态资源文件
  - `components/`: 可复用组件
  - `config/`: 配置文件
  - `hooks/`: Vue 组合式 API hooks
  - `i18n/`: 国际化相关文件
  - `router/`: 路由配置
  - `store/`: 状态管理
  - `utils/`: 工具函数
  - `views/`: 页面视图组件
  - `plugins/`: 插件配置
  - `composables/`: 组合式函数

## 重要文件

- [src/main.ts](mdc:src/main.ts): 应用程序入口文件
- [src/App.vue](mdc:src/App.vue): 根组件
- [vite.config.ts](mdc:vite.config.ts): Vite 构建配置
- [package.json](mdc:package.json): 项目依赖和脚本配置

## 开发规范

1. 组件文件使用 PascalCase 命名
2. 工具函数和 hooks 使用 camelCase 命名
3. 页面组件放在 views 目录下
4. 可复用组件放在 components 目录下
5. API 接口统一TsModel中
6. 用户实现功能时如果现有Api不能很好的满足,就必须拒绝执行提醒用户让后端实现接口
## 店铺平台类型
``` C#
  /// <summary>
  /// 店铺类型
  /// </summary>
  public enum ShopType
  {

      None = 0,
      //美团外卖
      美团 = 1,
      //饿了么外卖
      饿了么 = 2,

      美团闪购 = 3,
      美团医药 = 4,

      饿百零售 = 5,
      京东到家 = 6,

      抖店即时零售 = 7,

      //跟"饿了么"的区别是 这个是调用官方接口
      饿了么官方 = 8,

  }


```
### 其中 美团闪购,美团医药,饿百零售,京东到家,抖店即时零售 属于 零售类, 而美团,饿了么,饿了么官方 是属于餐饮类
